// src/automation/browser/types.js
// JSDoc type definitions for Playwright objects

/**
 * @typedef {Object} BrowserLaunchResult
 * @property {import('playwright').Browser} browser - Playwright browser instance
 * @property {import('playwright').BrowserContext} context - Browser context
 * @property {import('playwright').Page} page - Browser page
 */

module.exports = {};
